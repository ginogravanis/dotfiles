#!/usr/bin/env bash
# depends: coreutils, dmenu

configs=".local/bin
neovim
shell
bash
git
feh
X11
dwm
st
dmenu"

tty -s || termcmd="$TERMINAL -e"

choice=$(echo -e "$configs" | dmenu -i -p "Jump to config:")

case $choice in
   ".local/bin") $termcmd "$EDITOR" ~/.local/bin ;;
   "neovim") $termcmd "$EDITOR" "$XDG_CONFIG_HOME/nvim/" ;;
   "shell") $termcmd "$EDITOR" "$XDG_CONFIG_HOME/shell/profile" ;;
   "bash") $termcmd "$EDITOR" "$XDG_CONFIG_HOME/bash/bashrc" ;;
   "git") $termcmd "$EDITOR" "$XDG_CONFIG_HOME/git/config" ;;
   "feh") $termcmd "$EDITOR" "$XDG_CONFIG_HOME/feh/bg" ;;
   "X11") $termcmd "$EDITOR" "$XDG_CONFIG_HOME/x11/xinitrc" ;;
   "dwm") $termcmd "$EDITOR" ~/dev/dwm/PKGBUILD ;;
   "st") $termcmd "$EDITOR" ~/dev/st/PKGBUILD ;;
   "dmenu") $termcmd "$EDITOR" ~/dev/dmenu/PKGBUILD ;;
   *) ;;
esac
